# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
*.pkl
*.pickle

# Environment and IDE files
.env
.venv/
.ci-venv/
env/
venv/
.ipynb_checkpoints/
.vscode/
.idea/

# Local GCP credentials (never commit)
.gcp/

# Data and models (artifacts tracked via DVC/MLflow, not git)
data/raw/
data/processed/
models/
artifacts/
mlartifacts/

# Logs and outputs
logs/
runs/
mlruns/
notebooks/
*.log

# Big data files at repo root (optional)
*.csv
*.xlsx
# (removed the global *.json ignore so that params/metrics JSON can be versioned)

# DVC
# DO NOT ignore the whole .dvc directory – we need .dvc/config in git
.dvc/cache/
.dvc/tmp/
.dvc/config.local

# Docker and build
*.dockerfile
Dockerfile
__MACOSX/
*.egg-info/

# OS-specific files
.DS_Store
Thumbs.db

# Reports and HTML/PDF exports
reports/
*.html
*.pdf

# Legacy duplicates (cleanup)
#/models
#/reports
